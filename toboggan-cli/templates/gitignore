# Generated presentation artifacts. `toboggan build` writes the talk as
# `<slug>.toml`, hence the blanket rule — the hand-written configs are re-added.
*.toml
!mise.toml
!toboggan.toml
!.toboggan.toml
*.html
*.pdf
*.typ
/thumbnails/
/overview/
# What the GitHub Pages workflow builds into. `*.html`/`*.pdf` above already
# cover `dist/index.html` and `dist/presentation.pdf`, but `/overview/` is
# anchored at the root and misses `dist/overview/`.
/dist/
