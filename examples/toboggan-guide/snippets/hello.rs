fn main() {
    // Embedded into a slide by `<!-- code:rust:snippets/hello.rs -->`.
    // The file stays the single source of truth — edit it once, the
    // slide updates on the next build.
    println!("Hello from a Toboggan code embed!");
}
