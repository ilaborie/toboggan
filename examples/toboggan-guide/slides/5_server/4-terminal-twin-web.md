+++
title = "Live demo (web)"
classes = ["no_title", "wide"]
hidden_in = ["pdf"]
+++

# Live demo — web

This slide is **web-only** (`hidden_in = ["pdf"]`).

<!-- pause -->

Type a command — and keep typing: stepping through a slide does not restart the
shell.

<!-- term: . -->

<!-- notes -->
On the projector this is a real shell. In the printed/PDF handout it is
replaced by the static twin on the next slide.
